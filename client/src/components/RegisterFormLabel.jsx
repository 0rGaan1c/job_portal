import React from "react";

const RegisterFormLabel = ({ label }) => {
  return (
    <label className="label">
      <span className="label-text text-white">{label}</span>
    </label>
  );
};

export default RegisterFormLabel;
